#include <stdio.h>
    int main () {
        double h, km, r;
            scanf("%lf", &h);
            scanf("%lf", &km);

            r = (h*km)/12;
            
            printf("%0.3f\n", r);


    }
